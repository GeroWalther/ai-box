<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Hardened-runtime entitlements required for notarization of a WKWebView app. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- The microphone, for push-to-talk and dictation. Under the hardened
       runtime this is not optional: without it macOS refuses audio input to a
       signed build outright, which is why a dev build could hear and the
       installed one could not. -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
  <!-- Screen Assist acts on the Mac through Apple Events (volume, appearance,
       opening and quitting apps). Under the hardened runtime this is refused
       without the entitlement, silently. -->
  <key>com.apple.security.automation.apple-events</key>
  <true/>
  <!-- The app is both an HTTP client (LLM/providers) and a local server (phone access). -->
  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.network.server</key>
  <true/>
</dict>
</plist>
